@{
    Layout = "~/Views/Shared/_BaseLayout.cshtml";
}

@section Head
{
    @Styles.Render("~/Content/css")
    @RenderSection("Styles", false)
}

<nav class="navbar navbar-inverse" role="navigation">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-navigation-bar">
                <span class="sr-only">导航切换</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a href="javascript:void(0)" class="navbar-brand">
                <i class="glyphicon glyphicon-cog"></i> 管理控制台
            </a>
        </div>
        <div id="bs-navigation-bar" class="collapse navbar-collapse">
            <ul class="nav navbar-nav">
                <li class="active">
                    <a href="@Url.Action("Index", "Home", new { @Area = "Console" })">加密/解密</a>
                </li>
                <li>
                    <a href="@Url.Action("Index", "Install", new { @Area = "Console" })">安装数据库</a>
                </li>
            </ul>
        </div>
    </div>
</nav>
<div class="container">
    @RenderBody()
</div>

@Scripts.Render("~/bundles/common")
@RenderSection("Scripts", false)