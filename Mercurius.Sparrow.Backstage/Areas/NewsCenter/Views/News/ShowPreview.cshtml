@model Response<News>

@section Styles
{
    <link rel="stylesheet" type="text/css" href="~/Scripts/RichEditor/css/highlight.github.css"/>
    <style type="text/css">
        .news {
            width: 75%;
            margin: auto auto;
            margin-top: 10px;
        }

            .news .footer {
                border-top: 1px dotted #c0c0c0;
                padding-top: 5px;
                padding-bottom: 25px;
            }

            .news h4 {
                padding-bottom: 6px;
                border-bottom: 1px dotted #c0c0c0;
            }

            .news ul {
                width: 100%;
                list-style: none;
                padding-left: 0;
                display: inline-block;
            }

                .news ul > li {
                    float: left;
                    margin-right: 30px;
                }
    </style>
}

<div class="news">
    @if (Model.Data != null)
    {
        var attachments = HtmlHelperExtensions.GetBusinessFiles("新闻管理", Model.Data.Id.ToString());

        <h2>@Model.Data.Title</h2>
        <div>
            @Html.Raw(Model.Data.Content)
        </div>
        if (!attachments.IsEmpty())
        {
            var index = 0;

            <h4>文件下载</h4>
            <ul>
                @foreach (var item in attachments)
                {
                    <li><a href="@Url.GetFileUrl(item.File.SavedPath)">附件@(++index)：@(item.Description)</a></li>
                }
            </ul>
        }
        <div class="footer">
            <span class="pull-left"><b>发布者：</b>@Model.Data.PublisherName</span>
            <span class="pull-right"><b>发布时间：</b>@Model.Data.PublishDateTime.Value.ToString("yyyy-MM-dd HH:mm:ss")</span>
        </div>
    }
    else if (Model.IsSuccess)
    {
        <div class="alert alert-warning">
            <h3>新闻不存在！</h3>
        </div>
    }
    else
    {
        <div class="alert alert-danger">
            <h3>@Model.ErrorMessage</h3>
        </div>
    }
</div>