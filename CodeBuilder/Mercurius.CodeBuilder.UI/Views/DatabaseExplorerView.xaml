<UserControl x:Class="Mercurius.CodeBuilder.UI.Views.DatabaseExplorerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             Name="viewer"
             d:DesignHeight="300"
             d:DesignWidth="300"
             mc:Ignorable="d">
    <UserControl.Resources>
        <Style x:Key="{x:Type TreeViewItem}" TargetType="TreeViewItem">
            <Style.Resources>
                <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1" x:Key="{x:Static SystemColors.HighlightBrushKey}">
                    <GradientStop Offset="0" Color="#FFFF8F26" />
                    <GradientStop Offset="1" Color="#FFCC6D1D" />
                </LinearGradientBrush>

                <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1" x:Key="{x:Static SystemColors.ControlBrushKey}">
                    <GradientStop Offset="0" Color="#FFFF8F26" />
                    <GradientStop Offset="1" Color="#FFCC6D1D" />
                </LinearGradientBrush>
            </Style.Resources>
            <Setter Property="Padding" Value="15,6,60,6" />
            <EventSetter Event="MouseDoubleClick" Handler="OnOpenConnectedDatabase" />
        </Style>

        <Style TargetType="ContextMenu">
            <Style.Resources>
                <Style TargetType="MenuItem">
                    <Setter Property="Padding" Value="2,7,0,7" />
                </Style>
            </Style.Resources>
        </Style>

        <ContextMenu x:Key="dbExplorerContextMenu">
            <MenuItem Command="{Binding AddServerCommand}"
                      CommandParameter="{Binding RelativeSource={RelativeSource Mode=Self},
                                                 Path=.}"
                      Header="添加数据库连接...">
                <MenuItem.Icon>
                    <Image Width="16"
                           Height="16"
                           Source="../Images/New.png" />
                </MenuItem.Icon>
            </MenuItem>
        </ContextMenu>

        <ContextMenu x:Key="tableContextMenu">
            <MenuItem Click="OnUpdateConnectedDatabase"
                      Header="修改"
                      Tag="{Binding}">
                <MenuItem.Icon>
                    <Image Width="16"
                           Height="16"
                           Source="../Images/edit.png" />
                </MenuItem.Icon>
            </MenuItem>
            <Separator />
            <MenuItem Click="OnDeleteConnectedDatabase"
                      Header="删除"
                      Tag="{Binding}">
                <MenuItem.Icon>
                    <Image Width="16"
                           Height="16"
                           Source="../Images/delete.png" />
                </MenuItem.Icon>
            </MenuItem>
        </ContextMenu>
    </UserControl.Resources>
    <Grid>
        <TreeView Style="{x:Null}" BorderThickness="0,0,2,0" BorderBrush="#454545">
            <TreeViewItem IsExpanded="True" ItemsSource="{Binding ConnectedDatabases.Items}">
                <TreeViewItem.Header>
                    <ContentControl HorizontalAlignment="Stretch" ContextMenu="{StaticResource dbExplorerContextMenu}">
                        <ContentControl.Template>
                            <ControlTemplate>
                                <StackPanel HorizontalAlignment="Stretch" Orientation="Horizontal">
                                    <Image Width="32"
                                           Height="32"
                                           Source="../Images/Server.png" />
                                    <Label VerticalAlignment="Center"
                                           Content="数据库连接"
                                           FontSize="14"
                                           Padding="5,5" />
                                </StackPanel>
                            </ControlTemplate>
                        </ContentControl.Template>
                    </ContentControl>
                </TreeViewItem.Header>

                <TreeViewItem.ItemTemplate>
                    <DataTemplate>
                        <StackPanel ContextMenu="{StaticResource tableContextMenu}" Orientation="Horizontal">
                            <Image Width="24"
                                   Height="24"
                                   Source="../Images/Database.png" />
                            <TextBlock VerticalAlignment="Center" FontSize="12">
                                <TextBlock.Text>
                                    <MultiBinding StringFormat="{}{0}.{1}">
                                        <Binding Path="Type" />
                                        <Binding Path="Name" />
                                    </MultiBinding>
                                </TextBlock.Text>
                            </TextBlock>
                        </StackPanel>
                    </DataTemplate>
                </TreeViewItem.ItemTemplate>
            </TreeViewItem>
        </TreeView>
    </Grid>
</UserControl>
